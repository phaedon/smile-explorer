package(default_visibility = ["//visibility:public"])

cc_library(
    name = "tree_matchers",
    testonly = True,
    hdrs = ["tree_matchers.h"],
    deps = [
        "@googletest//:gtest",
    ],
)
